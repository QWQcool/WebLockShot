# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# 子路径部署回归（npm run e2e:basepath）的独立构建产物
dist-pages
*.local
.env
.env.*
# 例外：tldraw license key 官方明确「可以公开」（客户端本地验签，且本就在线上产物里），
# 有意提交以便**任何人 clone 后构建都与线上画面一致（无水印）**。理由与限制见文件内注释与 NOTICE。
!.env.production

# Local-only notes (keep on disk, never commit)
提示词存储.md
体验说明.md

# AI 助手工作目录：含本机路径与临时预览图，公开仓库不提交
.workbuddy/
.playwright-cli/

# 测试 Agent 冒烟临时快照与临时输出
canvas.yaml
.tmp-*
# 排查用临时脚本目录（曾被 oxlint 扫到而多出一条 no-unused-vars 警告，故显式忽略）
.tmp/
# 生产模式回归（npm run e2e:prod）的自签证书目录（已被 .tmp-* 覆盖，此处显式标注用途）
.tmp-prodmode/

# 伴生服务运行产物（剪映草稿 zip 落盘目录 / TTS 音频 / ffmpeg 成片）
jianying-drafts/
data/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
