# P0 + P1 开工任务书（执行方案）

> 用途：本文件是**抗上下文中断的交接凭证**。任何新会话读「本文件 + TODO.md」即可无缝接手。
> 目标：完成 TODO.md 的 **P0（移动端 / 触摸适配）** 与 **P1（节点执行历史面板）**，
> 随后统一**重拍截图 + 重建 PDF + 更新 HOW_TO_USE**。
> 建立时间：2026-09-11（基线 commit `bf901a2`，CI 双绿）

---

## 一、角色与协作规则

| 角色 | 谁担任 | 预算 | 职责 |
|---|---|---|---|
| 监管 / 协调 / 终验 | **主 Agent** | — | 阶段划分、spawn/重启成员、熔断判断、提交推送、最终验收 |
| 开发 | 团队成员 `wls-dev` | `max_turns=150` | 逐阶段实现 + 自验（门槛全过） |
| 测试 | 团队成员 `wls-tester` | `max_turns=100` | 独立验收（只读 + 独立测试文件）+ 防恒真抽查 |

**硬规则**
1. **严格串行**：开发完成 → 测试开始（并行编辑同一文件会静默覆盖，本项目已踩过）。
2. **不自行 commit**：开发/测试成员只交付代码与报告；**提交推送由主 Agent 统一执行**（提交信息需要整体视角 + 提交后要确认 CI）。
3. **报告落盘**：成员的关键结论记入 **本文件 §五 台账**（单一信息源，**不另建报告文件**，避免文档分散），不要只在消息里说（消息会失真）。

**熔断规则（防死循环）**
| 规则 | 内容 |
|---|---|
| 重启预算 | 每阶段：dev 最多重启 **2** 次、tester 最多 **1** 次；**全局重启 ≤ 4 次** |
| 无进展熔断 | 重启前记录「失败指纹」（错误消息 + 未完成项集合）；重启后报告指纹**相同 → 立即熔断**，不再重试 |
| 强制新信息 | 重启 prompt 必须包含「上次尝试了什么 / 为什么失败 / 这次换什么策略」，禁止空转重试 |
| 升级路径 | 1 次失败 → 换策略重启；2 次 → **主 Agent 自己动手**或缩小范围/降级实现；3 次 → **停止**并向用户报告（卡在哪 + 证据 + 已尝试 + 建议） |

---

## 二、阶段划分（严格按序）

### S1 · P0 移动端适配（CSS 为主）
- **问题与实测数据**：见 `TODO.md` P0 章节（390×844 下 129 个元素横向溢出、顶栏 223px、画布容器 969px、对话栏 943px、小地图 x=761 在视口外）。
- **根因**：`src/canvas/canvas.css` 只有一条 `@media (max-width: 768px)`，处理了节点面板与对话栏，**完全没处理顶栏**；顶栏按钮不换行不收缩，把整行撑到 900+px。
- **做法**（TODO.md 已列 5 条，按序）：
  1. 窄屏顶栏改**横向滚动一行**或**两行折叠 + 「更多」抽屉**；按钮加 `flex-shrink: 0` + `white-space: nowrap`，禁止文字竖排；
  2. `.wls-canvas-root` / `.wls-chat-dock` 加 `min-width: 0` + `width: 100%`，杜绝被子元素撑宽；
  3. 画布容器 `overflow: hidden` + 明确 `width: 100%`；窄屏节点面板改**底部横向滚动条**（现在是 `max-height: 9.5rem` 换行块，占 152px 高）；
  4. 窄屏隐藏小地图与快捷键提示；
  5. 触摸：核对 tldraw 手势（单指平移 / 双指缩放）；软键盘顶起时用 `100dvh` 而非 `100vh`。
- **验收标准**：
  - 390×844 与 360×640 两视口：**横向溢出元素 = 0**、`document.scrollWidth === innerWidth`；
  - 顶栏高度 **≤ 120px**，无竖排文字；
  - 画布区可见面积 **≥ 视口高度的 45%**；
  - 触摸可完成「进入画布 → 对话栏发一句话 → 节点落位」。

### S2 · `e2e:mobile` 套件断言化 + 纳入 CI
- **起点资产**：`scripts/e2e-mobile.mjs`（已转正的探针，当前只打印报告 + 截图，恒 exit 0）。
- **要补**：断言化（上述验收标准）+ 失败 `process.exit(1)` + `package.json` 加 `"e2e:mobile"` + CI workflow 接入。
- **负向验收（必须做）**：把 S1 的 CSS 还原回去，套件**必须立刻红**（防恒真）。

### S3 · P1 数据层：RunRecord 契约 + 存储 + 单测
- **结构**：`{ id, nodeId, kind, startedAt, endedAt, status, cost, error, outputRef }`；
- **单一收口**：由 `src/director/executor.ts` 在每个节点动作前后落一条（**不要在 UI 层各自记**）；
- **存储**：复用现有 IndexedDB 层（`src/persist/`），滚动保留最近 N 条（建议 200）+ 手动清空；
- **单测**：RunRecord 生成 / 裁剪逻辑。

### S4 · P1 UI：运行历史抽屉 + 钱包打通 + E2E
- **入口**：画布右下或顶栏「🕘 运行历史」，抽屉式列表；每条可点开看输入/输出摘要与失败原因；
- **与钱包打通**：直接显示该次消耗的灵感币与**是否发生退款**（本项目独有的信息，同类工具没有）；
- **诚实标注**：演示引擎产生的记录标注「演示 · 非真实生成」；
- **验收**：走完「脚本 → 分镜 → 出片」出现 ≥3 条（字段完整：状态/耗时/费用）；制造一次失败能看到失败原因且钱包显示已退款；刷新后历史仍在；E2E 断言面板可开合 + 记录数递增。

### S5 · 收尾：截图重拍 + PDF 重建 + HOW_TO_USE 更新
- 重拍：`npm run shots -- --prod`（**必须带 `--prod`**：https + 非回环域名，否则带 tldraw 开发水印）；
- PDF：`python docs/build_showcase_pdf.py docs/SHOWCASE.canvas.pdf`（并复制一份到桌面）；
- HOW_TO_USE：一主两分（`HOW_TO_USE.md` / `.en.md` / `.commerce.md` / `.commerce.en.md`）统一更新，含新增的「运行历史」入口与移动端说明。

---

## 三、门槛（每阶段必须全过，才算完成）

```bash
npm run lint        # 0 errors（基线 20 warnings）
npm run build       # 0 错
npm run test:node   # 基线 449（448 pass + 1 skip）
npm test            # 含 vitest 26
npm run e2e         # 17 步（S1/S2 后必须仍全过）
npm run e2e:mobile  # S2 之后（S1 期间可手动跑探针取证）
```

---

## 四、红线

1. **诚实标注**：未实现的能力界面里如实写「未实现」，不摆样例数据、不伪造评分；
2. **不引入任何绕过第三方许可校验 / 去水印的手段**（见 `NOTICE`）；
3. **不破坏 sell 6 镜管线**：`src/ai/`、`src/persist.ts`、`src/types.ts` 改动前先跑基线测试确认零回归（P1 允许改 `src/persist/` 目录内的存储层）；
4. **桌面形态零回归**：P0 是窄屏适配，1600×900 下渲染应保持不变（若变化，S5 重拍时同步更新）；
5. 收口一律 **commit + push 到 main**，并确认 CI（Deploy GitHub Pages / Docker Build Verify）双绿。

---

## 五、进度台账（主 Agent 维护）

| 阶段 | 状态 | 提交 | 备注 |
|---|---|---|---|
| S1 P0 移动端适配 | ✅ 完成 | S1 提交 | 顶栏 223→105px、画布容器 969×288→390×634（视口高 34%→75%）、对话栏 943→364px、节点面板底部横滚（152→59px）、窄屏隐藏小地图/快捷键、100dvh；覆盖 `index.css` 泄漏的全局小屏规则。桌面 1600×900 零回归（A/B 逐字段 0px）。独立验收 `wls-tester` **6/6 PASS**（含负向验收：换回 HEAD 版立刻变红） |
| S2 e2e:mobile 断言化 | ✅ 完成 | S2 提交 | 探针 → 断言套件 **15 条**（双视口×7 + 触摸1）：A 页面级 scrollWidth / B 有效溢出=0（实现比口径更严：`overflow-x∈{auto,scroll}` **且** `scrollWidth>clientWidth`）/ C 顶栏≤120 / D 无竖排（`TreeWalker SHOW_TEXT` 取行盒，规避装饰条假阳性）/ E 画布≥45% / F 小地图与快捷键隐藏 / G 触摸主流程；失败非 0 退出。CI：`e2e` job 新增 `Install CJK fonts` + 移动端断言两步（runner 无 CJK 字体，本套件断言文本度量布局）。独立验收 `wls-tester`：绿态 15/15、负向 12 条红、**定向变异 M1 只红 C / M3b·M4 只红 B**（断言独立且不过度排除真溢出） |
| S3 P1 数据层 | 待开始 | — | — |
| S4 P1 UI | 待开始 | — | — |
| S5 收尾（截图/PDF/HOW_TO_USE） | ✅ 完成 | S5 提交 | 新增 `19c_canvas_run_history.png`（混合方案：真跑 6 镜出片产 6 条真实记录 + 1 条演示失败记录，图注如实标注来源；Mock 引擎 0 币且从不失败，真实路径拍不出「费用/退款/失败原因」三列）；文档 13 个文件更新（含中英同步、7 处 stale 数字修正、README 已知限制补 400ms 缺口、HOW_TO_USE 一主两分加运行历史与移动端）；PDF 重建（SHOWCASE **11 页/11 图**、HOW_TO_USE **48 页**）。终验 `wls-tester`：5 项核心 PASS + 发现 3 处 stale（dev 补到 7 处） |

### 验收口径决策（S1 定稿，S2 起沿用）

1. **页面级（硬性）**：`document.documentElement.scrollWidth === window.innerWidth`；
2. **元素级**：**有效溢出 = 0** —— 即「排除有 `overflow-x: auto|scroll` 祖先的后代」后无溢出。
   理由：横滚容器是方案指定的做法，其内部元素天然超出视口右边界，原始口径会让任何横滚设计恒不过关。
3. **两条都过才算 PASS**；且元素级必须**逐项核验归属**（列出「溢出元素 → 可滚动祖先」清单），
   只要有一项无横滚祖先即 FAIL —— 防止口径放宽变成遮丑。

> 已知诚实抖动：390 视口下 `overflowCount` 会出现 52↔53 的 ±1 抖动（首个越界项 right 落在阈值附近，
> 亚像素/滚动位取整所致）。判定看「无横滚祖先」的数量（必须为 0），不看总数。
