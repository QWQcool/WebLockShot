# tldraw license key —— **有意公开**（不是泄露，也不是机密）
#
# 官方口径（https://tldraw.dev/sdk-features/license-key）：
#   "License keys are validated on the client. They can be public:
#    you can safely include them in your frontend code."
# 也就是说：key 本就在**每个访客的浏览器里**（线上产物已烘焙，谁都能取出来），
# 提交到这里只是让「任何人 clone 后 npm run build」得到与线上一致的画面（无水印）。
#
# ⚠️ 四个必须知道的限制
#   1. 这是本项目的**免费 trial key，2026-12-20 到期**。官方文档明确「评估（试用）许可
#      **没有宽限期**，到期立即失效」→ 到期当天线上画布会对**所有访客**停止渲染
#      （页面内有如实提示条说明原因与官方解决路径）。`npm run e2e:prod` 会在剩余 ≤30 天时告警。
#   2. 许可**绑定域名**（本 key 的允许域为通配 `*`）。部署到未覆盖的域名会被视为无许可。
#   3. **下游使用者请自行申请许可**。官方条款原文：
#      "If you wish to include tldraw in an open source project, you may do so but the SDK itself
#       must remain under its original license. This means that you and your downstream users will
#       require their own trial, commercial, or hobby license in order to use the SDK in production."
#      试用许可每个商业主体限一个 —— **请不要把本 key 用于你自己的生产环境**。
#   4. 本项目**不提供任何绕过许可校验 / 去水印手段**（见 NOTICE）。
#
# 优先级：CI 构建时由 `TLDRAW_LICENSE_KEY` secret 注入同名环境变量覆盖本文件
# （Vite 的进程环境变量优先于 .env 文件），因此线上始终用仓库 Secrets 里的值。
VITE_TLDRAW_LICENSE_KEY=tldraw-2026-12-20/WyJfTTkyeERmVyIsWyIqIl0sMTYsIjIwMjYtMTItMjAiXQ.7U4UkU3YTCCWoMJMlKxtDBH5Tc07R1gEHdHQaPoqoU/Q2JmH2Givyos7+rHi2dXkwc9GNDnJfA/iD/dcFXtHDw
