# 画布性能基准（CANVAS_PLAN.md §9 T3）

> 生成：2026-09-11 03:22:13 · 由 `npm run perf` 实测产出（本文件为生成物，请用脚本刷新而非手改）

## 实测环境

- 浏览器：chromium headless · 视口 1600×900
- 构建：复用 dist（--skip-build） · 渲染：软件光栅（无 GPU 加速）

## 基准表

| 场景 | 指标 | 数值 | 单位 | 备注 |
|---|---|---|---|---|
| 3D 运镜台 | three 懒加载 chunk 体积 | 957.1 | kB | Stage3DViewport-DVHmqYmX.js |
| 构建产物 | dist/assets JS 合计 | 3404.1 | kB | 5 个 chunk |
| 画布 200 节点 | 批量创建耗时 | 17.7 | ms | 单一 editor.run batch + zoomToFit |
| 画布 200 节点 | 持续平移帧率 | 55.7 | fps | 均值 17.96ms · P95 32.3ms · 111 帧 |
| 画布 200 节点 | 落盘节点数 | 200 | 个 | 契约上限 200 节点 |
| 画布 500 节点 | 追加 300 节点耗时 | 62.3 | ms | 200 → 500 |
| 画布 500 节点 | 持续平移帧率 | 26.5 | fps | 均值 37.72ms · P95 48.4ms · 53 帧 |
| 画布 500 节点 | 落盘节点数 | 200 | 个 | 契约 max 200 → 超限静默不落盘（见结论） |
| 记忆图谱 500 记录 | 打开到渲染完成 | 90 | ms | 含 IndexedDB 读取 + 聚合 + 布局 |
| 记忆图谱 500 记录 | 渲染节点数 | 16 | 个 | 聚合桶 + Top 叶，节点数与记录数非 1:1 |
| 记忆图谱 500 记录 | 记录数口径 | 500 | 条 | 图谱脚注如实展示 |
| 3D 运镜台 | 点击到外壳可见 | 115 | ms | 含懒加载 chunk 拉取 |
| 3D 运镜台 | 点击到视口就绪 | 352 | ms | WebGL 就绪 |
| 3D 运镜台 | 懒加载 chunk 网络加载 | 3.4 | ms | Stage3DViewport-DVHmqYmX.js 957.3kB |
| Skill 市场 100 项 | 打开到 100 卡片渲染 | 88 | ms | localStorage 读取 + 校验 + 渲染 |
| Skill 市场 100 项 | 已安装卡片数 | 100 | 个 |  |
| Skill 市场 100 项 | 市场面板 DOM 节点数 | 1340 | 个 | 全量渲染，未做虚拟列表 |

## 口径（可复现）

- **帧率**：在页面内用 `requestAnimationFrame` 逐帧 `editor.setCamera({x: x+1})` 持续平移画布
  2000 ms，取 rAF 间隔均值的倒数；headless 软件渲染下不具 GPU 代表性，只用于同机回归对比。
- **节点创建**：单一 `editor.run` batch 创建 + `zoomToFit`，不含 tldraw 首帧渲染等待。
- **记忆图谱**：先向 IndexedDB 灌 500 条真实结构记录（与回流看板同库），再刷新打开图谱，
  计时到 `.mg-canvas` 出现（含读取 + Laplace 聚合 + 极坐标布局）。
- **3D 冷加载**：新浏览器上下文（无 HTTP 缓存），点击「进入 3D 运镜台」到视口就绪。
- **Skill 市场**：向已安装库灌 100 条合法 manifest 后刷新打开，计时到 100 张卡片全部渲染。

## 结论与优化建议（本期只测不改）

1. **画布文档契约上限 = 200 节点 / 400 边**（`canvasDocSchema`）。超过上限时
   `validateCanvasDoc` 返回 null，`persistNow` 静默提前返回——**画布看起来正常但不再落盘**，
   且 UI 无任何提示。这是当前最值得处理的诚实性问题，建议：超限时在保存状态位如实提示
   「节点数超过契约上限 200，本次未保存」（不扩契约、不静默）。
2. **边提取是 O(n²)**：`editorPageToCanvasDraft` 内 `shapes.find(...)` 按 shapeId 线性查找，
   节点/边规模增大时落盘耗时随之放大。建议改为一次 `Map<shapeId, shape>` 索引。
3. **Skill 市场全量渲染**：100 项即渲染 1340 个 DOM 节点，
   未做虚拟列表/分页。规模继续增长时建议加窗口化（与既有「万行网格」同思路）。
4. **记忆图谱节点数与记录数解耦**（聚合桶 + Top 叶上限），500 条记录不会线性放大渲染成本——
   当前设计已具备规模弹性。
