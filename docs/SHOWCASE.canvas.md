<div class="cover">
  <div class="kicker">项目能力展示 · Agent 创意画布</div>
  <h1>WebLockShot · 一句话到成片工程</h1>
  <p class="lead">在浏览器里跑完的创意生产工作台：在无限画布上用一句话布置 Agent 节点，
  从脚本一路做到<b>可直接导入剪映继续剪辑</b>的成片工程。</p>
  <div class="cover-meta">
    <span>在线体验：<b>https://qwqcool.github.io/WebLockShot/</b></span>
    <span>技术栈：React 19 · TypeScript 6 · Vite 8 · tldraw 5 · three.js · GSAP</span>
    <span>形态：纯静态可部署 · 免安装 · 免 API Key 即可跑通全链路（演示引擎）</span>
  </div>
</div>

打开链接即可完整体验，**不需要注册、不需要配置任何 Key**：对话栏说一句话，画布上会自动长出一条
Agent 节点链路；点几下就能拿到脚本、9:16 分镜预演、逐镜出片，最后打包出一份**能导入剪映的工程文件**。

<div class="stats">
  <div class="stat"><b>9</b><span>类 Agent 节点</span></div>
  <div class="stat"><b>6</b><span>个生成引擎契约</span></div>
  <div class="stat"><b>45</b><span>个 3D 动画片段</span></div>
  <div class="stat"><b>15</b><span>步实机 E2E</span></div>
  <div class="stat"><b>429</b><span>项自动化测试</span></div>
  <div class="stat"><b>50k+</b><span>行自有代码</span></div>
</div>

这不是一个「界面原型」：它包含**可运行的执行引擎、真实的两阶段结算事务、真实的文件产出**，
并且每一条能力都有对应的自动化测试与实机验证记录。

> **关于本文件的诚实声明**：所有数字与截图均来自仓库内可复现的脚本实测，不含设计稿或手绘 mock。
> 凡是尚未实现的能力，产品界面里同样会如实标注「未实现」，本文件也照实写出（见第五章）。

![产品主界面：顶栏（模式切换 / Skill / 记忆图谱 / 连接器 / 创作场景 / 中英切换）+ 左侧节点面板 + 无限画布 + 底部对话栏 + 右下小地图](screenshots/16_canvas_workbench.png)

## 一、端到端链路：一句话 → 成片工程

这条链路不是流程图上的箭头，而是**能在浏览器里点完**的真实路径：

| 步骤 | 用户动作 | 系统实际做了什么 |
|---|---|---|
| ① 需求 | 对话栏输入一句话（或点场景模板） | LLM 编排；**无 Key 时自动降级为演示编排并如实标注「非真实 LLM」** |
| ② 落位 | —— | 按场景路由在画布上布置节点拓扑与连线（可一键整批撤销） |
| ③ 脚本 | 点「生成脚本」 | ScriptWriter 产出脚本 + Critic 对立面评审（含评分与修改建议） |
| ④ 分镜 | 点「生成分镜」 | 拆成 6 镜，生成 9:16 的 **GSAP 动态分镜预演**（本地渲染，可播放） |
| ⑤ 出片 | 点「逐镜出片」 | ExecutorEngine 串行队列逐镜执行；**钱包先冻结、成功核销、失败全额退款** |
| ⑥ 交付 | 点「打包剪映草稿」 | 生成三轨对齐的 `draft_content.json` + 完整素材 zip，**真实下载** |

![开场层：五类创作场景 tab + 大输入卡，首访叠加引导](screenshots/15_canvas_onboarding.png)

![对话栏一句话 → 自动布置节点拓扑（提示条如实标注「演示编排 · 非真实 LLM」）](screenshots/17_canvas_orchestration.png)

![9:16 动态分镜预演：分镜节点内嵌 GSAP 播放器，逐镜可播放（本地预演 · 非成片）](screenshots/04_step3_gsap_storyboard.png)

## 二、三个差异化能力

### 2.1 3D 运镜台：把「运镜」变成可预演、可复用的本地能力

同类创意画布大多止步于 2D 出图。这里内置了一套**本地 3D 预演台**——全程在浏览器里用 three.js 渲染，
**不消耗任何积分、不依赖云端 GPU**：

- **内置 CC0 素体**：Quaternius Universal Animation Library，模型内嵌 **45 个真实动画片段**（实测值），
  其中 8 个做成动作预设（待机 / 走路 / 慢跑 / 冲刺 / 跳舞 / 坐姿 / 跳跃 / 说话）；
- **六套程序化场景预设**：商品台 / 影棚 / 客厅 / 卧室 / 户外台阶 / 展台，一键切换；
- **多机位 + 关键帧轨迹**：机位可增删、可预览，导演视角参数可提取，关键帧驱动运镜；
- **首/尾帧导出**：直接把摆好的画面接给「视频生成」节点做单镜直出，或接给「分镜预演」出自由镜数分镜。

![3D 运镜台：CC0 素体 + 影棚场景预设 + 多机位 + 关键帧时间轴（本地渲染 · 0 灵感币）](screenshots/20_canvas_stage3d.png)

### 2.2 剪映 / CapCut 草稿 zip 一键交付

多数工具交付的是「一段 mp4」，拿到后想改就只能重做。这里交付的是**可以继续剪的工程**：

- **三轨对齐**：视频轨 + 旁白音轨 + 花字字幕轨，时间轴**微秒级对齐**；
- **完整素材包**：`draft_content.json` 与所用素材一起打包成 zip，解压放进剪映草稿目录即可打开；
- **两条落盘路径**：纯前端直接下载 zip；伴生服务在线时还可一键解压到本机草稿目录。

![剪映草稿导出：draft_content.json 三轨对齐 + 完整素材 zip（真实文件产出）](screenshots/13_jianying_zip_export.png)

### 2.3 诚实标注 + 可验证工程

这一条看起来「不性感」，但它决定了这个项目**能不能被信任**：

- **不伪造能力**：无 Key 时标注「演示编排 · 非真实 LLM」；未实现的节点标「未实现」；
  空态不摆样例数据；第三方许可限制如实告知并给出官方解决路径（且**不提供任何绕过手段**）。
- **不伪造测试**：15 步实机 E2E 走的是真实鼠标路径；测试环境不可用时**优雅跳过并打印指引**，不伪装通过。
- **不放过线上事故**：两起线上事故（子路径资源 404 白屏、许可闸门致画布消失）在本地全套测试下**都是绿的**
  ——因为本地形态与线上不同。修复后各自补了**复现线上形态**的回归套件（见第五章）。

![记忆图谱：真实回流记录可视化；无数据时如实显示空态，不摆样例数据](screenshots/19b_canvas_memory_graph_data.png)

## 三、工程实现

### 3.1 分层结构

| 层 | 职责 | 关键模块 |
|---|---|---|
| 入口层 | 开场层 / 对话栏 / 场景模板 | `ui/canvas/CanvasOnboarding`、`wls-chat-dock` |
| 编排层 | 一句话 → 节点拓扑 | 场景路由 + LLM 编排（可降级演示编排） |
| 画布层 | 无限画布 + 9 类节点 | tldraw 5 + 自研 `shapeUtils`、CanvasDoc 契约、localStorage 持久化 + 多标签同步 |
| 执行层 | 串行队列 + 结算 | `director/executor`、`domain/wallet`（两阶段事务） |
| 能力层 | 脚本 / 分镜 / 出片 / 重绘 / 3D | `ai/`（ScriptWriter + Critic）、`media/`（6 引擎契约）、`ui/stage3d/` |
| 交付层 | 产物卡 + 剪映工程 | `export/jianyingDraft` + 零依赖 zip |
| 支撑层 | 本地优先 / 可选伴生服务 | IndexedDB 大资产、`server/`（TTS / ffmpeg / sqlite / MCP） |

### 3.2 几个值得说的工程决策

- **本地优先**：API Key 只存浏览器，画布与素材默认不离开本机；纯静态即可部署，伴生服务是**可选项**。
- **钱包全站唯一收口**：画布生成与带货工作台走**同一条**钱包管线——冻结 → 核销 / 全额退款，
  含幂等结算与 30 分钟 TTL 孤儿冻结自动回收。
- **节点连线带类型契约**：非法连线会被拒绝并给出提示条，避免「连了但跑不通」。
- **3D 资源走统一 URL 解析器**：子路径部署（GitHub Pages）下不会因写死根路径而 404（这条是事故换来的，见 4.2）。
- **零依赖伴生服务**：`server/` 不引第三方运行时，剪映草稿落盘 / TTS / ffmpeg 合成 / MCP 桥接全用 Node 标准库。

### 3.3 代码规模（实测）

| 部分 | 文件数 | 行数 |
|---|---|---|
| 前端 TS/TSX（`src/`） | 185 | 33,899 |
| 样式（`src/**/*.css`） | 8 | 8,244 |
| 伴生服务（`server/`） | 16 | 5,187 |
| 测试与工具脚本（`scripts/`） | 15 | 3,315 |

## 四、质量与验证体系

### 4.1 自动化测试矩阵（全部可本地复现）

| 套件 | 命令 | 覆盖 | 当前结果 |
|---|---|---|---|
| 单元测试（node） | `npm test` | 钱包事务 / 执行引擎 / 6 引擎契约 / 剪映草稿 / 3D 契约 / i18n / 画布契约 | **411 项，0 失败** |
| 组件测试（vitest） | `npm run test:ui` | 画布关键组件渲染 | **18 项，0 失败** |
| 画布 E2E | `npm run e2e` | 真实鼠标路径 15 步（含节点管线**实跑**与悬浮提示对比度守卫） | **15/15** |
| 子路径部署回归 | `npm run e2e:basepath` | 复现 GitHub Pages `base=/WebLockShot/` 形态 | **全过** |
| 生产模式回归 | `npm run e2e:prod` | HTTPS + 非回环域名，验证许可闸门与诚实提示 | **全过** |
| 降级 / 迁移矩阵 | `npm run degrade` | 六类能力缺失路径优雅降级 | **6/6** |
| 跨浏览器 + a11y | `npm run a11y` | chromium / webkit 双引擎 + axe-core（WCAG 2.0 A/AA） | 严重项 **0** |
| 性能基准 | `npm run perf` | 画布交互与渲染基准 | 见 `docs/perf.md` |

### 4.2 两起线上事故的复盘（这部分是加分项）

| 事故 | 现象 | 根因 | 修复与防回归 |
|---|---|---|---|
| 子路径资源 404 | 线上点「加人物」**整页白屏** | 3D 模型 URL 写死根绝对路径 `/models/…`，在 `/WebLockShot/` 子路径下 404，`useGLTF` 抛错且无兜底 | 统一 `publicUrl` 解析器 + 根级/局部双重 ErrorBoundary + **新增子路径部署回归套件**；并做**负向验收**（还原错误写法 → 套件 5 项失败并复现 404） |
| 许可闸门致画布消失 | 线上画布内容**突然全部消失** | tldraw 生产环境无 license key 时约 5 秒后停止渲染编辑器（本地 http/localhost 被豁免，**所以本地永远测不出来**） | 加如实提示条（挂在编辑器之外，闸门后仍可见）+ **新增生产模式回归套件**（HTTPS + 非回环域名复现线上形态，双向断言） |

> 这两条的价值不在「修好了」，而在于：**定位到「本地测试面与线上形态不一致」这个系统性盲区**，
> 并把两种线上形态都纳入回归。此后同类问题在本地就会红。

## 五、已知边界与路线（如实写出）

工程能力也体现在**敢写出自己没做什么**：

| 项 | 现状 |
|---|---|
| 移动端 / 触摸适配 | **尚未适配**。实测 390×844 视口下顶栏被压成 223px 高、画布容器宽 969px 溢出视口（129 个元素横向溢出），画布区基本不可用。已列入待做清单（P0） |
| 图像生成节点 | 占位（未实现），界面如实标注；补齐需接入文生图 / 图生图链路 |
| 真实生成质量 | 默认走**演示引擎**（不耗积分、可离线跑通全链路）；真实出片需自备引擎 Key 或本地 ComfyUI |
| 3D 动作预设 | 8 个常用动作；「挥手 / 转身」在内置动画库里无对应片段，**如实替代、不伪造** |
| 画布引擎许可 | tldraw 为商业许可（非开源）。当前用免费 trial，**2026-12-20 到期**；仓库已提交 `.env.production` 使任何人构建都无水印，但下游使用者需自行申请许可 |
| 多人协作 | 未做（单机本地优先；协作属 tldraw 付费能力） |

**路线（按性价比排序）**：移动端适配 → 节点执行历史面板（可回看的运行日志）→ MCP 工具清单产品化
（让外部 Agent 驱动画布）→ 提示词库多来源导入。

## 六、60 秒上手

```bash
# 在线直接体验（免安装、免 Key）
https://qwqcool.github.io/WebLockShot/

# 本地跑起来
git clone https://github.com/QWQcool/WebLockShot.git
cd WebLockShot && npm install
npm run dev            # 打开 http://localhost:5173

# 质量门禁（可自行复现上面第五章的全部数字）
npm test               # 单元测试
npm run e2e            # 15 步实机 E2E
npm run e2e:basepath   # 子路径部署回归
npm run e2e:prod       # 生产模式回归
```

![中英双语：顶栏一键切换并持久化，英文界面同样完整可用（非截图贴图，真实 i18n 字典）](screenshots/23_canvas_en.png)

<div class="endnote">
<b>一句话总结</b>：这是一个「从一句话到可继续剪辑的成片工程」的端到端实现，
自带 3D 本地预演与可验证的工程体系；它也清楚地知道并写下了自己还没做什么。
</div>
